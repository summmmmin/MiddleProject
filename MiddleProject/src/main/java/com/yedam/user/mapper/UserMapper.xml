<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.users.mapper.UsersMapper">

<!-- 로그인 -->
<select id="loginCheck" parameterType="com.yedam.users.domain.UsersVO" resultType="com.yedam.users.domain.UsersVO">
    SELECT * FROM users WHERE userId=#{userId} AND userPw=#{userPw}
</select>

<!-- addMember -->
<insert id="addUsers" parameterType="com.yedam.users.domain.UsersVO">
    INSERT INTO members (user_id, user_pw, user_nm, user_phone, user_add, user_grade, user_date, user_point) 
    VALUES (#{userId}, #{userPw}, #{userNm}, #{userPh}, #{userAddr}, '일반회원', sysdate, 0)
</insert>
</mapper>